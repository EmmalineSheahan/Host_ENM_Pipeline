#!/bin/sh 
#SBATCH --account=guralnick
#SBATCH --qos=guralnick
#SBATCH --job-name=Host_ENM_accessible_area_nat   #Job name 
#SBATCH --mail-type=ALL   # Mail events (NONE, BEGIN, END, FAIL, ALL)
#SBATCH --mail-user=sheahane@ufl.edu   # Where to send mail      
#SBATCH --cpus-per-task=1   
#SBATCH --nodes=1
#SBATCH --ntasks=1
#SBATCH --mem-per-cpu=80gb
#SBATCH --array=1-22
#SBATCH -t 72:00:00   # Walltime
#SBATCH -o Host_ENM_accessible_area_nat.%j.out   # Name output file 
#
pwd; hostname; date

# Set the number of runs that each SLURM task should do
PER_TASK=27

# Calc the starting and ending values for this task based on the SLURM task and the num of runs per task
START_NUM=$(( ($SLURM_ARRAY_TASK_ID - 1) * $PER_TASK + 1 ))
END_NUM=$(( $SLURM_ARRAY_TASK_ID * $PER_TASK ))

# Print the task and run range
echo This is task $SLURM_ARRAY_TASK_ID, which will do runs $START_NUM to $END_NUM

# Run the loop of runs for this task.
for (( run=$START_NUM; run<=END_NUM; run++ )); do
# Export these values as environmental variables that R can read
export START_NUM=$run

  echo This is SLURM task $SLURM_ARRAY_TASK_ID, run number $run

# Load in R:

module load R

echo "beginning natural range taxa for accessible area"

Rscript scripts/acc_area_making.R

done